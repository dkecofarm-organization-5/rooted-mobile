# Timing Manager status

The current BGM RC8.1 distribution does **not** include a visitor-facing Timing Manager UI.

The local server's timing-save validation has been updated so that, if a compatible manager is connected later, scene count is validated dynamically against the active runtime instead of the legacy 89-scene count.

Current runtime: 94 scenes per language.
